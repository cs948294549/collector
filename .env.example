# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=network_monitor

# 数据目录配置
COLLECTOR_DATA_DIR=/root/docker_apps/snmp_collector

# SNMP 配置（可选）
SNMP_COMMUNITY=public
SNMP_VERSION=2
SNMP_TIMEOUT=5
SNMP_RETRIES=2

# 并发配置（可选）
MAX_WORKERS=50
BATCH_SIZE=100
